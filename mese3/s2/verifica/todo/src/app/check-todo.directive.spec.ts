import { CheckTodoDirective } from './check-todo.directive';

describe('CheckTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTodoDirective();
    expect(directive).toBeTruthy();
  });
});
